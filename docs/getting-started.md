# Getting Started

Start with the root [setup guide](../setup.md). It walks through the full first-time setup from downloading the repository, installing Arduino CLI, flashing the ESP32, joining the setup Wi-Fi, and deploying the optional Cloudflare Worker remote dashboard.
